export interface PermisoI{
    id_per?: string,
    fecha_per: string,
    id_usuario: string,
    motivo_per: string,
    aprobado_per: string,
    fechaapro_per: string,
    token?: string,
    nombre?: string,
    mail?: string
}